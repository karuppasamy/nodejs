{

  "development": {
    "port": "{{= process.env.OPENSHIFT_NODEJS_PORT || 8080 }}",
    "ip_address": "{{= process.env.OPENSHIFT_NODEJS_IP || '127.0.0.1'}}"
  },

  "test": {
    "port": 3000
  },

  "production": {
    "port": "{{= env.PORT }}"
  }

}
